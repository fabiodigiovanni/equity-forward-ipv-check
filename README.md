# equity-forward-ipv-check
A lightweight Python IPV check for equity forwards: recompute a carry-based forward, cross-check with an options-implied forward via put–call parity, and generate a control-grade report (ΔF, tol_eff, implied net carry, Δq bps + root-cause hints).
